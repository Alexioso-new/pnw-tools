PNW TOOLS v113 (label v9.13) — KEAMANAN & KEBERSIHAN app.js
===========================================================

1) KEAMANAN — akun lokal hardcoded DICOPOT
   - Sebelumnya bundel publik memuat username+password admin/pengurus dalam
     bentuk teks polos (siapa pun bisa baca lewat View Source).
   - Kini login admin HANYA lewat Firebase: isi email+password admin di modal
     login, atau tombol "Masuk dengan Google".
   - Sesi akun lokal lama (tersimpan di perangkat) otomatis dibersihkan sekali
     saat versi ini pertama dibuka — siapa pun yang sebelumnya "masih login"
     lewat akun lama akan diminta login ulang lewat Firebase.
   - Catatan: login admin kini butuh koneksi internet (Firebase). Tidak ada
     lagi pintu masuk offline.

2) KEBERSIHAN — kode mati youTh Views lama dihapus dari js/app.js
   - Fungsi siaran teks/ayat lama (tombolnya sudah tidak ada sejak v82),
     helper pembaca panel proyektor lama, dan watcher kanal CastFlow di
     app.js (output sudah pindah total ke castflow.html sejak v83).
   - Jalur Spectate (?mode=stage / ?mode=display) TIDAK disentuh dan tetap
     berfungsi penuh.

3) PERSIAPAN BLAZE + STORAGE (tugas pengguna di Firebase Console)
   - Berkas baru: storage.rules (aturan Cloud Storage) + CARA-PASANG-BLAZE.txt
     (panduan langkah demi langkah).
   - Setelah Blaze aktif dan rules dipasang: rekaman latihan online + Buku
     Panduan PDF langsung berfungsi (kodenya sudah siap sejak v64/v65).

Teknis:
- js/app.js menyusut ~110 baris; tidak ada perilaku UI yang berubah selain
  teks label login ("Username / Email" -> "Email").
- Tidak ada berkas runtime baru; APP_SHELL tidak berubah.
- Tidak perlu publish RTDB rules baru.

Deploy:
Timpa C:\pnw-tools (pertahankan .git) -> git add -A -> git commit -m v113 ->
git push -> buka web -> Ctrl+Shift+R -> label v9.13 -> login ulang lewat
Google/email Firebase.

QA: 22/22 (index.html 16/16 + castflow.html 6/6).
