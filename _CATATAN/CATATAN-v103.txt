CASTFLOW v103 (label v9.3) — SPRINT 4: TOAST SYSTEM + WORKSPACE + DIAGNOSTICS
=============================================================================

Melanjutkan v102 — Sprint 4 dari dokumen rencana (UI System & Workspace
Foundation). Isi:

- TOAST SYSTEM TERPUSAT — semua notifikasi kini lewat satu sistem (antrian
  maksimal 3, auto-dismiss, klik untuk tutup, ARIA). Toast lama otomatis
  ikut lewat jembatan di projector.js — call site lama tidak diubah.
- WORKSPACE MANAGER (fondasi) — menu Avatar: preset layout (Default,
  Wide Preview, Focus Design, Focus Lyric), Reset Layout, dan Save Layout
  Snapshot. Preset ditulis dalam format grid legacy yang sama lalu reload —
  tidak ada format baru yang dikarang.
- DIAGNOSTICS — menu Avatar -> "Diagnostics": status app, output (live),
  preflight terakhir + tombol Run, media hilang + tombol Scan, error terbaru,
  dan jumlah key storage. Semua dibaca dari store — panel tidak menyimpan
  state sendiri.

PERAPIKAN FILE CATATAN (permintaan pengguna):
- Format ZIP TIDAK berubah — sama seperti v102 ke belakang (file di root).
- Yang menumpuk di C:\pnw-tools adalah 18+ file CATATAN-v85.txt s/d v102.txt
  (ZIP hanya menimpa, tidak pernah menghapus). Cara merapikan:
  SETELAH menimpa C:\pnw-tools, dobel-klik BERSIHKAN-CATATAN.bat — semua
  CATATAN-v*.txt + CARA-PASANG-*.txt + database.rules.json pindah ke satu
  folder _CATATAN\ yang rapi. Aman dijalankan berkali-kali.

BERKAS BARU: js/cf-toast.js, js/cf-workspace.js, js/cf-diag.js, css/cf-v103.css,
             BERSIHKAN-CATATAN.bat
DIPATCH: js/projector.js (bridge toast), castflow.html, sw.js, .assetsignore,
         label versi v9.3, cache pnw-tools-v103.

URUTAN DEPLOY (rules v101 sudah dipublish — tidak perlu diulang):
1. Ekstrak ZIP -> salin SEMUA isinya -> timpa ke C:\pnw-tools (.git utuh).
2. (Opsional) dobel-klik BERSIHKAN-CATATAN.bat untuk merapikan catatan lama.
3. git add -A && git commit -m "v103" && git push
4. Ctrl+Shift+R -> label v9.3.
5. Uji: Go Live (toast kanan bawah); Avatar -> Layout -> Wide Preview;
   Avatar -> Diagnostics menampilkan Output: connected.
