PNW TOOLS v114 (label v9.14) — PEMISAHAN CSS TAHAP 2 (css/yv.css)
==================================================================
Tahap struktural terakhir dari rencana pemisahan CastFlow (bagian 25 memori).

1) css/yv.css — lembar gaya khusus CastFlow
   - Seluruh 196 aturan ber-selektor proj* dipindahkan dari css/design.css
     ke css/yv.css (termasuk 24 aturan di dalam @media responsif).
   - Dimuat HANYA oleh castflow.html, tepat setelah design.css (urutan
     cascade identik dengan sebelumnya — tidak ada perubahan tampilan).
   - index.html TIDAK lagi memuat aturan proj* apa pun.

2) Manfaat isolasi
   - Bug CSS aplikasi utama (editor chord, jadwal, menu) tidak bisa lagi
     menyentuh layar ibadah CastFlow — menutup kelas bug cascade v81 secara
     struktural.
   - Blok campuran (selektor gabungan proj + non-proj, 6 di design.css + 1
     penjaga [hidden] di styles.css) sengaja dibiarkan: castflow.html memuat
     kedua stylesheet sehingga tetap bekerja, dan index.html masih butuh
     bagian non-proj-nya.
   - Penjaga null openProjBtn ternyata sudah ada sejak lama (terverifikasi).

3) Verifikasi
   - Diff pixel: screenshot castflow.html & index.html SEBELUM vs SESUDAH
     pemisahan — identik (RMSE ~0).
   - QA headless: regresi penuh kedua halaman.

Teknis:
- Berkas baru: css/yv.css + CATATAN-v114.txt.
- sw.js: cache pnw-tools-v114, APP_SHELL += ./css/yv.css.
- Tidak ada perubahan JS selain label versi. Tidak perlu publish rules.

Deploy:
Timpa C:\pnw-tools (pertahankan .git) -> git add -A -> git commit -m v114 ->
git push -> buka web -> Ctrl+Shift+R -> label v9.14.
